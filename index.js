const RandomFunction = (fixednumber) => {
    let random = Math.floor(Math.random() * fixednumber);
    return random;
}

console.log(RandomFunction(443));

// The Function above generates random numbers, rounds it up to a whole number and return the value.

// You can as well Modify it given Arrays of Data and you want to randomly select a Winner

//WAGMI